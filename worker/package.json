{
  "name": "@bondibai/worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:integration": "node test/integration.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260515.1",
    "typescript": "^5.9.2",
    "vitest": "^5.0.0",
    "wrangler": "^4.131.1"
  }
}
